name: Release to latest/edge

on:
  push:
    branches:
      - main

jobs:
  vm-integration:
    name: Integration tests
    runs-on: ubuntu-latest
    uses: ./.github/workflows/vm_integrate.yaml

  release-to-charmhub:
    name: Release to CharmHub
    needs:
      - vm_integration

    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Select charmhub channel
        uses: canonical/charming-actions/channel@2.0.0-rc
        id: channel
      - name: Upload bundle to edge
        uses: canonical/charming-actions/upload-bundle@1.0.0
        with:
          bundle-path: releases/latest/vm/postgresql-bundle.yaml
          credentials: "${{ secrets.CHARMHUB_TOKEN }}"
          github-token: "${{ secrets.GITHUB_TOKEN }}"
          channel: "${{ steps.channel.outputs.name }}"
